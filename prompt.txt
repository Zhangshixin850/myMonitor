perp_price_monitor.py是一个监控asterdex和我binance共5种币价以及保证金率的python脚本，可以稳定运行多日。请先仔细分析一下逻辑和架构，不做任何改动。
我想在这个脚本的基础上往里面添加一些功能：1.将监控的币种槽位扩展为8个。2.可监控两个不同的asterdex账号的保证金率。3.可监控这8个币种的费率，如果费率低于年化-x%则告警。4.将.env.monitor文件名改为.env
要求：
1.尽量减少REST请求频率，多利用已有的返回数据。
2. 不要过度拆分函数，只有逻辑会被多处复用时，才抽取独立顶层函数。尽量扁平化。
3. 仅调用一次的简短逻辑直接写在主流程，不要封装成微型辅助函数，避免流程在大量小函数之间跳转，增强可读性，不要教科书式过度模块化。
4. 注释只保留必要的、简洁的说明。
5. 代码总体要简洁易读易维护，消除冗余。
